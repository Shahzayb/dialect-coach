# History

Append-only. One row per plan and implementation, oldest first. Newest entry last; never rewrite a row except to move its status from `planned` to `implemented`.

| Date | Plan file | Description | Status |
| --- | --- | --- | --- |
| 2026-08-17 | `plans/2026-08-17_project-scaffold.md` | Runnable scaffold: placeholder Streamlit page, pinned env, config + deploy files. Python 3.12 (`pydub` needs stdlib `audioop`, removed in 3.13). Exact `==` pins verified against PyPI, not recalled — streamlit 1.61.1, azure-cognitiveservices-speech 1.51.1, google-genai 2.18.1, pydub 0.25.1, python-dotenv 1.2.3, pydantic 2.13.4; no numpy, no pytest yet. Docker became the primary run path mid-implementation (host stays clean, native Azure SDK deps pinned); `python:3.12-slim` is Debian trixie, so `libasound2`→`libasound2t64` and the Dockerfile accepts either — a missing one is an opaque `ImportError`, not a build failure. `requirements.txt` kept as the single manifest because HF Spaces reads exactly that. Deliberately left out: all seven feature modules (not even empty stubs), `tests/` and the Azure fixture, secret loading, budget meters. Unverified and deferred to deploy: `packages.txt` names vs the Space image, `sdk_version: 1.61.1` vs HF's supported list. | implemented |
| 2026-08-17 | _no plan file (user waived it)_ | `make setup`/`make up`/`make down` added, thin Makefile wrapping `docker compose`. `setup`'s logic (create `.env` from `.env.example` if missing, verify `docker compose` is on PATH) went into `scripts/setup.py` instead of shell, per the user's standing preference for Python over `.sh` once there's any branching. Verified live: `make setup` is idempotent on an existing `.env`, `make up` builds and serves 200 on :8501, `make down` tears down cleanly. README and `techContext.md` updated to point at the `make` targets instead of raw `docker compose` commands. | implemented |
| 2026-08-17 | _no plan file (user waived it)_ | `scripts/smoke_test.py` added: one-off script making one real Azure STT call (`recognize_once` on a locally generated 16 kHz/mono WAV) and one real Gemini call, to confirm `AZURE_SPEECH_KEY`/`AZURE_SPEECH_REGION`/`GEMINI_API_KEY` work end-to-end before building anything on them. Run via `docker compose run --rm app python scripts/smoke_test.py <wav>`, since only the container has the pinned deps. Test audio was generated host-side with macOS `say --file-format=WAVE --data-format=LEI16@16000` (Linux container has no TTS) and deleted after the run. Both calls passed: Azure transcribed the phrase correctly on the first try; Gemini 404'd on `gemini-2.5-flash` (retired for new users per the API's own error) and passed once switched to `gemini-3.6-flash`. Script is a standalone diagnostic, not wired into the app. | implemented |
| 2026-08-18 | `plans/2026-08-18_azure-analysis-core.md` | Azure analysis core: `utils.py`, `audio_utils.py`, `speech_analyzer.py`, `db.py`, `budget.py`, a minimal Drill/Paragraph UI, and offline tests against a real captured Azure fixture. Modes A (single-shot, `enableMiscue` on) and B (continuous, `enableMiscue` off — Azure ignores it there, so omissions/insertions are diffed locally with `difflib` and marked `source: local_diff`). Mode B merge is duration-weighted, never a naive mean; completeness is recomputed globally rather than averaged, and `pron_score` as a weighted average is an approximation since Azure's composite weighting is unpublished. SQLite stores **both raw API responses verbatim** — `azure_raw_json` now, `gemini_raw_json`/`coach_source` created NULL so the coaching chunk is an UPDATE, not a migration; no audio ever. Usage meter derives from the attempts table, so `BUDGET_STATE_PATH`/`.usage.json` is dropped — one store, no drift. `OFFLINE_MODE` bypasses the F0 tier check and budget guard, otherwise the zero-cost path would be harder to use than the paid one. Deliberately out: Gemini coaching, fallback coach, `phoneme_reference.py`, TTS, Mode C, and the rich §11 UI. Found the hard way, all contradicting the master plan or the docs: the SDK nests `ErrorType` and `Feedback` *inside* each word's `PronunciationAssessment`, so reading `word["ErrorType"]` parses every word as clean — a silent perfect score; delivery faults (`UnexpectedBreak`/`MissingBreak`/`Monotone`) are not `ErrorType` values but live under `Feedback.Prosody`; and `enable_content_assessment_with_topic` does not exist anywhere in SDK 1.51.1, which blocks Mode C's content scoring as designed. Review caught seven more, the worst being that the local miscue diff indexed the word list with indices from a re-tokenised string — a punctuation-only word silently dropped a real word from the results, a hyphenated word shifted every later index. Also fixed: secret redaction never reached tracebacks (filters run before the formatter renders `exc_text`), retries were unmetered despite re-uploading the audio, and the result cache evicted by insertion order, dropping exactly the entry the drill retry loop re-uses. pytest 9.1.1 pinned; collection scoped to `tests/` because `scripts/smoke_test.py` has `test_`-prefixed functions that make real billable calls. 108 tests, all offline with no keys. | implemented |
| 2026-08-18 | `plans/2026-08-18_legible-audible-diagnosis.md` | Make the diagnosis legible and audible: master plan §11's UI plus §6's "Hear it" playback, as one chunk — they are inseparable because a playback button lives next to each flagged word. Colour-coded reference text against the `utils.py` thresholds, the reference-vs-heard diff, expected → produced IPA per flagged word, the syllable/stress line, the delivery panel aggregating `UnexpectedBreak`/`MissingBreak`/`Monotone`, a new `tts.py`, and per-word plus whole-text playback cached in `st.session_state` by `(voice, text, slow)`. Landed as planned, plus one thing the plan did not foresee: `tts.payload_for` was extracted so the pre-flight guard prices exactly the string that gets billed instead of re-deriving the SSML. Decided with the user up front: "Hear it" renders **disabled** under `OFFLINE_MODE` rather than faking audio (that contract stays absolute — no network call, ever); voice comes from `AZURE_TTS_VOICE` only, no picker; slow playback (SSML `prosody rate="-35%"`) is in scope. Verified against the installed SDK rather than recalled: `SpeechSynthesizer`'s `audio_config` defaults to a **default-speaker output object, not `None`**, so omitting it synthesises to a device the container does not have and `audio_data` never comes back — the TTS equivalent of the `apply_to` trap. `speech_analyzer._classify_cancellation` becomes public and takes a `bad_request_hint`, so `tts.py` reuses one error map and `QuotaExhausted` stays a single type. Out, unchanged: the whole coaching layer and Mode C. 161 tests, all offline with no keys, plus a live run against the real `.env` and the 12.8 s recording: prosody came back populated (76.4), TTS returned real 24 kHz WAV, the slow path measured 1.54× the normal duration exactly as `rate="-35%"` predicts, and four playback clicks produced three `tts_usage` rows because the repeat was served from cache. Review caught four issues, the worst being that a TTS 403 crashed the page: `QuotaExhausted` subclasses `AssessmentError`, not `PermanentError`, so `play()`'s except clause never caught it and the `mark_quota_exhausted` branch inside that handler was unreachable. Also fixed: the audio player matched on word text rather than the clicked widget, so a word flagged twice in a paragraph would start the same clip in two players at once. A second review pass against the *running* app found four more: every failure path in `play()` rendered its alert inside the button's `st.columns` entry (measured at 124 px in a 672 px row — one word per line), a run that failed after retries charged the meter nothing despite the comment beside it saying attempts count even when they fail, the pre-flight priced one attempt against a charge of up to three, and omitted words — the ones the speaker demonstrably could not say — were the only ones with no "Hear it" button. 166 tests. | implemented |
| 2026-08-18 | `plans/2026-08-18_coaching-layer.md` | The coaching layer: `phoneme_reference.py`, `fallback_coach.py`, `ai_coach.py`, and the UI that renders a structured report. Build order is deliberate — the deterministic fallback is the *primary* path (the free Gemini tier runs out, and a report built from Azure's own data is fully testable offline with no key), so the report schema is defined once in `fallback_coach.py` and both coaches emit it, giving the UI one renderer. Decided with the user up front: the fallback renders free on every assessment and an "Improve this with Gemini" button spends one call on demand, so no assessment burns free-tier RPD unasked and the send to Google is a choice rather than a side effect; the pure normalised-data readers (`phoneme_pairs`, `is_flagged`, `delivery_summary`) move from `app.py` into `speech_analyzer.py` so the word card and the coaching report cannot disagree; no minimal-pair playback in this chunk. Verified before writing code rather than recalled: `models/gemini-3.6-flash` is still live (free `models.list()` call, and `gemini-3.7-flash` now exists as an env-var swap), google-genai 2.18.1 carries `response_schema` and converts the required pydantic shape cleanly via `t_schema`, and Azure's IPA is rhotic and length-mark-free (`ɝ ɚ ɹ ɔɹ ɪɹ`, never `iː ɑː ɜː`) so the reference table keys on Azure's symbols behind an alias map. | implemented |
| 2026-08-19 | `plans/2026-08-19_record-assess-defects.md` | Five interaction defects on the record-and-assess surface (GitHub #4–#8, milestone v0.2.0) plus the 8 open code-review findings from the coaching layer. Reset button, delete-recording control, `Assess` disabled while in flight, a `Stop` button, and collapsing 100-score-but-flagged words. Researched Streamlit 1.61.1 mechanics before designing rather than assuming: a new widget interaction cannot interrupt a blocking call already in progress, so the Azure call moves to a background `threading.Thread` with the main script polling via `time.sleep`+`st.rerun()`; widget resets use `on_click`/`on_change` callbacks per Streamlit's own documented pattern, and `audio_input`/`file_uploader` are keyed on a generation counter because neither can be cleared through session state (still an open upstream request). Metering rule decided explicitly and implemented: a cancelled run never writes an `attempts` row and is never billed, whether or not it reached Azure — the cancel check sits before `db.record_attempt` and the meter derives from that table; the existing "a completed run counts every attempt it made" rule is untouched. Only continuous recognition can be stopped mid-call (`stop_continuous_recognition_async`); single-shot has no SDK abort, so a stop there discards the result on arrival and the UI says which of the two happened rather than implying it interrupted anything. Verified against the installed SDK rather than recalled: **no httpx transport exception subclasses the builtin `TimeoutError`/`ConnectionError`** (checked all six in the container), so `_classify`'s original test misread every real network failure as permanent and skipped the retry it documented — now keyed on `httpx.TransportError`. Also confirmed live in the browser: ten rapid `Assess` clicks produce exactly one attempt row. 293 tests, all offline. `use_container_width` swapped for `width="stretch"` in `app.py` — it is past its removal date and the new 0.4 s poll loop would have spammed the deprecation warning on every rerun. | implemented |
| 2026-08-19 | `plans/2026-08-19_surface-scores-and-metrics.md` | Milestone v0.3.0: #11 (score breakdown), #13 (per-word breakdown layout), #10 (mispronunciation/break/monotone tracking), pronunciation half of #12 (banded scores + mispronunciation counts) — content half of #12 (vocabulary/grammar/topic) is out of scope, scripted assessment never returns it. Pure render-layer work against `assessment.overall_scores`/`assessment.words`, no API cost. Confirmed before planning: `render_delivery` already covered breaks/monotone (#10's only real gap was the mispronunciation count); #11 and #12's pronunciation half are the same "Score breakdown" widget; #13's image is a hover tooltip on the colour-coded running text, not the flagged-word cards. Added `utils.AzureBand`/`azure_score_band()` — Azure's own 0-59/60-79/80-89/90-100 convention, kept explicitly distinct in a comment from the existing `WORD_RED`/`WORD_AMBER`/`PHONEME_RED`/`PHONEME_AMBER` heuristics; banding stays presentation-only, `overall_scores` keeps raw floats. Added `speech_analyzer.mispronounced_words()`. Rewrote `render_scores` (banded Pronunciation headline + Completeness metric + Accuracy/Fluency/Prosody bars, "—" never `0` for a missing score) and added `render_error_counts` (four count badges, count-only — `render_delivery` and the flagged-word cards keep the per-word detail, not duplicated). Replaced `colour_coded_html`'s plain `title=` tooltip with a real CSS `:hover` tooltip (`word_tooltip_html`) reusing `speech_analyzer.phoneme_pairs`. Found live in the browser rather than assumed: Streamlit 1.61.1 exposes no CSS custom properties for its theme anywhere in the DOM, so the tooltip uses one fixed light card (verified legible on both themes) instead of a `var(--...)` that would never resolve. 311 tests (up from 293), all offline. Verified live in the browser against the real drill fixture (3 real `Mispronunciation` words, pron 83/accuracy 89/fluency 88/prosody 76.4) on both the light and dark Streamlit theme; the fixture has no real break/monotone examples (pre-existing gap), so those three badges were checked at 0 live with non-zero behaviour covered by tests, consistent with existing precedent. GitHub bookkeeping (splitting #12, closing #10, closing the milestone) deliberately left for the user to confirm separately. | implemented |
| 2026-08-19 | `plans/2026-08-19_prosody-coaching-payload.md` | Milestone v0.4.0 (#9): make the prosody score actionable. Delivery faults (`UnexpectedBreak`/`MissingBreak`/`Monotone`, which live under `Feedback.Prosody` and not in `ErrorType`) become a distinct section of the existing coaching payload — aggregated span plus the `BreakLength` and `SyllablePitchDeltaConfidence` values beside them — and the report gains a `delivery_drills` section that must come back as something to perform, not a description. Templated per-fault advice in `fallback_coach` is what makes it work with no API key. Not a new model call and not a second request. `BreakLength`'s unit is unverified (every value in the committed fixture is 0), so no unit is asserted and the measurements are deliberately kept out of the ranking. The committed fixture carries no delivery fault, so coverage is against a hand-built payload marked synthetic, plus a new `OFFLINE_FIXTURE` override so the offline exit criterion runs through the real UI. Explicitly not here: numeric rhythm/pitch measurement from phoneme durations and an F0 track — a later, complementary chunk. | implemented |
| 2026-08-19 | `plans/2026-08-19_progress-view-benchmark.md` | Milestone v0.5.0: the progress view — the first feature that reads the SQLite history back. New `progress_view.py` (pandas + altair, never imports Streamlit, same pure-helper boundary as `app.py`'s render helpers), a `Progress` tab beside `Practice`, and the four scores plus flagged-phoneme and flagged-word aggregates plotted over time. The headline decision is the **benchmark passage**, chosen once here for two consumers — this chart and the vowel-measurement calibration read a later chunk needs — because plotting scores across arbitrary self-chosen texts measures text difficulty, not the speaker. 198 words, ~79-85 s, covering the commonly substituted consonants and the full en-US vowel inventory in stressed unreduced contexts; the coverage tables ship as `BENCHMARK_COVERAGE` data with a test asserting every listed token really appears, so the justification cannot drift from the text. Benchmark attempts are identified by matching the normalised `reference_text` rather than a new column — `db._migrate` has no upgrade path and the v1 precedent says not to add one. Mode A and Mode B never share a line, enforced structurally: only the benchmark subset (always paragraph mode) gets a line mark, free practice is unconnected points shaped by mode, and a test asserts no `line` layer encodes `mode`. `pandas`/`altair` pinned explicitly since `progress_view.py` imports them directly rather than relying on the transitive gift from streamlit. Two things found while building rather than planned: the coverage table caught its own drift immediately — "which" was listed under /w/ and /ɪ/ but had been edited out of the passage, which is the entire argument for shipping the justification as data instead of prose; and the seed script's first version replayed the committed weather fixture against the benchmark reference, so the Mode B miscue diff marked two hundred words omitted on every benchmark read and "the" and "i" topped the flagged-word ranking — seeded benchmark rows now carry a payload built from the passage itself. Versions read out of the built image, not recalled: pandas 3.0.5, altair 6.2.2. `use_container_width` avoided on the new charts (past its removal date, as the record-assess chunk already found), and Vega drops every other axis label on a tight band, so the rankings set `labelOverlap=False`. 392 tests, all offline. The 30-day check is explicitly a calendar item, not a merge gate: the benchmark series starts empty on the day this ships. | implemented |
| 2026-08-19 | `plans/2026-08-19_timing-data-and-npvi.md` | Milestone v0.6.0: carry Azure's `Offset`/`Duration` (100-ns ticks, present at word, syllable AND phoneme level) plus the top-level `SNR` through `speech_analyzer._normalise_word`, which discards all of it today, and compute nPVI (normalised Pairwise Variability Index) over vowel durations in a new pure `rhythm.py`. The de-risking chunk for all later accent work — rhythm, vowel-space drift, an F0 track, slicing audio — and valuable alone. Purely additive: no consumer reads a normalised word by position, no stored shape changes, and `progress_view.parse_attempts` already re-parses `azure_raw_json` through `normalise` on every Progress render, so every stored attempt gains timing and an nPVI retroactively with no migration. The headline decision is **which comparison is primary**: published GA nPVI bands come from hand-segmented corpora reading different material, so scoring Azure-derived durations against one compares three things at once. Measured on the committed fixture, four defensible segmentation choices give 50.3/54.75/55.72/56.25 on the *same unchanged recording* — a 5.4-point spread from policy alone. The primary comparison is therefore the same benchmark passage through Azure TTS and the same pipeline, captured once in this chunk (975 TTS chars, ~85 s STT), and the published band gets no chart ink at all. Audio storage: the user's override of the no-audio rule is recorded in the memory bank but deliberately not built — no column, `db.SCHEMA_VERSION` stays 1. | implemented |
| 2026-08-19 | `plans/2026-08-19_perception-trainer-practice-queue.md` | Milestone v0.7.0: the perception trainer (`perception_trainer.py`) and the practice queue (`practice_queue.py`) as one chunk — the queue decides what is trained and the trainer is the first thing it schedules. High Variability Phonetic Training: forced-choice identification of minimal pairs across at least four en-US voices varied in sex and timbre, scored immediately, ~20 trials a block. Voice variety is the active ingredient, not a garnish, so a block refuses to run under four voices rather than degrading. TTS only, no STT spend at all. Decided with the user before planning: a new **first tab `Today`** as the single entry point (textarea one click away); **no L1 hint** — with no flagged history the queue says so and offers nothing, keeping "the queue must never invent a target" absolute and `projectbrief.md`'s no-hardcoded-L1 non-goal intact; **all three item kinds now**; and **"unseen item" means an unheard `(word, voice)` combination**, which gives 24-40 novel stimuli per contrast against `phoneme_reference`'s 3-5 minimal pairs. Two findings from exploration reshaped the scope: a **vowel gap needs no formant work** — it is a flagged substitution whose expected phoneme is a vowel/diphthong/r-coloured in `phoneme_reference`, so it trains through the same block as a consonant contrast and only its label and reason differ; and **Azure emits no stress marks** (verified against the fixtures — `unpredictable` returns `ʌn/pɹə/dɪk/tə/bəl` with accuracy scores and nothing else), so a stress target has no scored check that costs no STT. Resolved the way the brief already names — "the due block, the due drill": contrast/vowel items get a scored block graduating on accuracy, stress items get the existing `fallback_coach` drill and graduate on **evidence drying up** (the word stops being flagged), with both rules rendered on screen carrying their real numbers. A CMUdict-backed stress-*location* task is the recorded upgrade path, not built. New tables `practice_targets` and `perception_trials` are additive `CREATE TABLE IF NOT EXISTS`, so `SCHEMA_VERSION` stays 1 — `alternatives` is stored **per trial** so the chance floor is a stored fact rather than an assumption in the reader, and every accuracy figure is reported beside it (a 2AFC scores 50% by guessing). Synthesised audio is cached to disk under `data/`, keyed by (voice, text, rate), plain text never SSML — the meter bills markup, and one 8-character word wrapped in SSML measured 167 characters; the rule this does not violate is the **user's** audio, never cached. Cost re-measured against the actual corpus: `phoneme_reference` holds 88 contrasts and 280 pairs over 282 distinct words (1,152 chars), so the whole corpus is ~4,600 characters at six voices, not the brief's ~2,000 estimate — still under 1% of the monthly allowance, and paid lazily per block. **Landed as planned, with four things found while building.** (1) The roster is six voices, not four: `scripts/list_voices.py` confirmed all four originally hardcoded names exist and that the listing charges nothing (meter 0 before, 0 after), but Andrew/Ava/Brian/Emma are all the same conversational generation and share a recording character — a single-talker block wearing a disguise — so it is now three male and three female across two generations, with Brian left out because it is `AZURE_TTS_VOICE`'s default. (2) Selecting the best twenty stimuli and *then* ordering them cannot guarantee voice rotation: selection can hand back eight of one voice, and no ordering of eight-in-twenty avoids a repeat. The pool is dealt round-robin instead so rotation is a property of the selection — and the pair-avoidance preference has to search within one novelty tier only, or a cosmetic choice about presentation order quietly returns a stimulus already heard while an unheard one waits. (3) The first browser check printed the whole graduation rule twice three inches apart, because `Decision.reason` restated what `render_target_card` renders directly above it — the same padding the delivery drills already taught to avoid. (4) AppTest executes `app.py` as its own module object, so a monkeypatch on the test file's imported `app` never reaches the running script; the Today tab is covered through the real path (two attempts of the committed capture) instead, which is the better test anyway since promotion coming out of stored attempts is the thing worth proving. **Measured live against Azure on 2026-08-19:** a fresh 20-trial block on `/θ/ → /t/` needed 38 clips and charged 167 characters, one `tts_usage` row per clip with none double-charged; a second block on the same contrast drew different stimuli from the same pool and charged 9 characters for the 2 clips it had never played, the other 36 served from disk. Synthesis is sequential at ~1 s per clip, so a fresh block takes ~40 s to prepare before the first trial — paid once per contrast, instant on a warm cache. 556 tests, all offline. | implemented |
| 2026-08-19 | `plans/2026-08-19_shadowing-practice-flow.md` | Milestone v0.8.0: shadowing — practise against a synthesised model in real time, then assess that recording as an ordinary Mode B attempt (no new analysis path, no new normalised shape). Two modes, only ONE assessed: simultaneous (one continuous clip, comparable to a cold read) is assessed; echo (per-sentence clips concatenated with a silence matched to each clip's own duration) is a warm-up only, because its structural pauses would depress fluency by design and make the delta uninterpretable — that finding is what decided the split. Tagging uses a new additive `attempt_tags` table (`CREATE TABLE IF NOT EXISTS`, `SCHEMA_VERSION` stays 1, the v0.7.0 precedent); `rhythm.BASELINE_CAPTURE_MARKER`'s reference-text prefix is NOT reusable here, since the comparison pairs shadowed to cold by matching that very text. Correctness crux: `is_benchmark` matches on text, so a shadowed read would land on the headline trajectory and the nPVI series as if it were cold — `progress_view.cold_attempts()` keeps it off both, while the flagged aggregates deliberately keep including it (`_tally` counts attempts, so an assisted read can only raise a count, never retire a target early). Scheduling is a fourth `shadow` kind in `practice_targets` that never graduates and is created on first use, never by `promote()`. `MAX_DURATION_SECONDS_PARAGRAPH` 120 → 180: slow shadowing of the 196-word benchmark is ~95 s of model audio plus lead-in. Pre-registered finding: shadowed should beat cold on fluency/prosody and the gap should NARROW over weeks; a gap that never narrows means the practice is not transferring and is to be reported as such. Built and verified offline — 632 tests (up from 556), and the whole surface driven in the browser on the seeded demo in both themes. `app.synthesise_clip` was extracted from `buy_block_audio` so the block and the shadow model charge the meter by one rule. Two existing tests were rewritten rather than patched: the score chart now carries two line layers (the rule it actually enforces is that no line encodes `mode`), and the preset selectbox is addressed by key since Today now renders one ahead of it. Altair refuses a facet whose layers have different data sources, so the zero rule is drawn from the same frame with `alt.datum(0)`. Live TTS verified against the meter: the model clip charged one row of 975 characters and came back 61.775 s (the committed baseline says 61.8 s), a second preparation charged nothing, and the echo track was 14 clips / 959 characters / 129.15 s against a predicted 129.2 s. **The first real shadowed read then found two defects the offline suite could not reach, both from the same root — two surfaces can now start an assessment and Streamlit runs every tab body on every rerun**: `last_key` is a single slot, so the Practice tab re-rendered the shadow result and `render_result`'s attempt-derived widget keys collided (`StreamlitDuplicateElementKey`, fixed with a `result_owner` slot); and a queue holding nothing but a shadowing passage read as a full one, reporting "nothing due, they are all on the review schedule" about targets never promoted (fixed by keying the empty state on `practice_queue.promotable`). The read stored clean and tagged — 66.5 s, pron 84.1, fluency 87.2, prosody 69.9 — but **there is still no cold read of the passage, so the pre-registered finding has no data**. Total live spend: 66 s STT, 6,283 TTS characters. | implemented |
| 2026-08-20 | `plans/2026-08-20_src-layout-linting-typing-ci.md` | Milestone v0.9.0: the tooling chunk — #17 (move source under `src/`), #15 (Ruff format + lint), #18 (mypy), #16 (GitHub Actions), in that order and as one chunk because all four touch the same 9,582 lines and sequencing them apart means three passes over every file with three chances for a real change to hide inside churn. Placed deliberately after the feature work and before the audio-measurement chunks: moving files and switching on mypy while feature branches are in flight is a merge-conflict generator, and this way the strictness is switched on before the newest and most numerically delicate code gets written. Decided with the user up front: **bare-root `src/*.py`**, not a `dialect_coach` package, so `import utils` is unchanged and each runner gets exactly one resolution mechanism (Streamlit takes the script's own directory, pytest takes `pythonpath = src`, `scripts/` keep a retargeted `sys.path` shim, mypy takes `mypy_path`, ruff takes `src`); **CI runs the Docker image** rather than a native pip install, so the apt list for the Azure SDK's native deps stays only in the Dockerfile and CI is bit-for-bit `make test`; **no Python version matrix** despite #16 asking for one, because `.python-version`, the Dockerfile and `requirements.txt` all pin 3.12 deliberately (pydub needs the stdlib `audioop`, removed in 3.13) and nothing has ever run on 3.11; and a **hard socket guard** in the suite on top of the existing offline guards. The controlling constraint is that a CI run must be structurally unable to spend quota, argued as four independent layers any one of which suffices: `ci.yml` references no `secrets.*` at all and uses `pull_request` rather than `pull_request_target`; a CI checkout has no `.env` and compose declares it `required: false`; `tests/conftest.py` deletes the three keys and forces `OFFLINE_MODE` for every test; and the new autouse fixture raises on any non-loopback `socket.connect`. Sizing was measured with `uvx ruff`/`uvx mypy` before planning rather than estimated: `ruff format` reflows 45 files, `ruff check` finds 124 (83 auto-fixable), and mypy at strict finds 13 across the four pure modules named in the brief with five more modules already at zero. `line-length = 100`, not ruff's default 88 — the codebase is hand-wrapped near 96, so 2,035 lines exceed 88 but only 23 exceed 100. Rules left out on purpose: ANN (mypy's job), T20 (scripts print legitimately), ARG (Streamlit callbacks), TRY, N818 (renaming `TierNotAcknowledged` is a public API change), UP042 (StrEnum changes `str()` on values written to SQLite), B905, RUF015 (`next(iter(x))` raises StopIteration, not IndexError), and RUF001-003 (the IPA symbols and en/em dashes are deliberate). Sizing also found one real latent bug: `tests/test_app.py` annotates `stop: "threading.Event"` in a module that never imports `threading` at module scope — a string annotation, so the suite passes today and would fail under `typing.get_type_hints`. **Landed with five things worth knowing.** (1) The `src/` layout is **bare-root**, so not one import statement changed — the whole cost was retargeting five path references, and `speech_analyzer.FIXTURE_DIR` was the single line of source that had to move with it (it derived `tests/fixtures/` from `Path(__file__).parent`, which had been the repo root). (2) `ruff format .` reformats Python fenced inside Markdown and rewrote the code blocks in five `plans/` files on the first run; those are records of what was intended at the time, not source, so ruff is now confined with `include = ["*.py", "*.pyi"]`. (3) `RUF100` deleted twelve `# noqa: BLE001` comments as dead — and with them twelve sentences explaining why each blind `except` is deliberate. `BLE` is now selected because the codebase was already written for it; ruff flags only four of the twelve sites, so five keep the noqa and seven keep the sentence as a plain comment, verified by diffing the set of reasons against the previous commit. (4) **mypy went strict on every module under `src/`, `app.py` included.** The plan budgeted for leaving the UI layer loose, but with the dependencies actually installed app.py cost ten signature annotations, so it went on the tier with everything else; `tests/` and `scripts/` stayed off it, where global strictness raises 284 findings that are almost all "annotate the pytest fixture parameter". (5) The 25 `# noqa: E402` in `scripts/` are gone: ruff recognises the `sys.path` bootstrap and never flagged E402 there, so they were dead. **What the tools found that was actually wrong**, as opposed to unannotated: `tests/test_app.py` annotated `stop: "threading.Event"` in a module that never imported threading at module scope (a string annotation, so it passed and would fail under `typing.get_type_hints`); `shadowing.phrases` was annotated `text: str` while its body does `(text or "")` and a test asserts `phrases(None) == []`; `fallback_coach` bound `words` twice in one function, once as `list[str]` and once as `str`; eleven tests indexed `db.get_attempt(...)` without checking it for None; and four pytest fixtures were annotated as the value they yield rather than as iterators. Two type facts are now written down rather than assumed — `utils.RowLike`, a Protocol for what a row reader actually needs (subscript by column name; `sqlite3.Row` is not a `Mapping`, which is why `progress_view.is_shadowed` already caught both `KeyError` and `IndexError`), and the Azure/Google SDK boundaries annotated `Any` explicitly, which is what lets everything around them be strict. The socket guard is the layer below `OFFLINE_MODE`: the flag is a decision a future path could forget to make, and a socket that refuses is not a decision — the four test files that deliberately switch `OFFLINE_MODE` off passed under it unchanged, which is the result worth having. 641 tests (637 plus the four guarding the guard), and the churn commit's count is identical to the commit before it. Verified live in the browser on a seeded 30-day database: the Today queue, all six Progress charts, and loading a drill preset, whose text arrives byte-identical — asserted by parsing both versions of `app.PRESETS` with `ast`, since `progress_view.is_benchmark` matches attempts on that text and one changed character would silently start a second series. | implemented |
| 2026-08-20 | `plans/2026-08-20_accent-measurement-engine.md` | Milestone v0.10.0: the accent measurement engine — the gradient measurement Azure's categorical phoneme scores cannot express. Four instruments from one slice-and-measure loop (vowel position in Lobanov space, 20→80% trajectory, F3−F2 rhoticity, duration/reduction), a speaker baseline calibrated from two reads of the existing `BENCHMARK_PASSAGE`, and the paired-read displacement stored as the measurement noise floor so nothing smaller than it is ever reported as change. Dependencies settled against primary sources before planning: `praat-parselmouth` 0.4.7 publishes **no linux-aarch64 wheel at any Python version** (all 100 files enumerated) so the arm64 container compiles Praat from the 22.5 MB sdist via scikit-build — taken anyway, because route two (scipy + hand-rolled LPC) cannot resynthesise and would silently delete v0.11.0's modified-self-voice surface. One correction to the brief found in the sdist: parselmouth exposes **no `Manipulation` class binding**, so v0.11.0 must be planned against untyped `parselmouth.praat.call`. `cmudict` 1.1.3 for lexical stress (pure data, no nltk), measured at 128/128 benchmark words and 100/101 word alignment against the committed fixtures rather than assumed. Three of the brief's own numbers corrected from `vowdata.dat`: the reference samples at **20/50/80%, not 25/75** (adopted, so the bias is zero); GA /eɪ/ F2 travel is ~140 Hz, not 620; and pre-fortis clipping has **no published target at all** because every Hillenbrand stimulus is `/hVd/`. The reference covers 12 of the 22 categories the passage carries, so ten report position with an honest "no published GA reference". The memory-bank contradiction resolved **against** the brief's premise: `techContext.md` records the user lifting the no-stored-audio rule on 2026-08-19, so `projectbrief.md` is current and two code docstrings asserting the old constraint are the stale text. User's decision: keep every recording. | **Landed as planned, with the dependency fears overblown and three of the brief's own numbers corrected.** The Praat compile is 100 s, not the tens of minutes 535 `.cpp` files suggested. The reference samples at 20/50/80% (adopted, so the bias is zero rather than documented), GA /eɪ/ F2 travel is ~140 Hz and not 620, and pre-fortis clipping has no published target at all because every Hillenbrand stimulus ends in a voiced /d/. CMUdict alignment came out at 101/101 rather than the predicted 100/101, because considering every pronunciation variant rescues *our*. Two defects found by running it rather than reasoning about it: the vowel chart shipped its first browser check with no colour legend (altair merges layered legends, so `legend=None` on the label layer removed both, and the surviving size legend made the loss easy to miss), and `isolated_db` cleared `st.cache_resource` but not `st.cache_data` while the progress view and practice queue key cached reads on `(highest attempt id, row count)` — two modules each seeding two attempts collide exactly, which surfaced as thirteen unrelated practice-queue failures with no hint of a caching cause. 732 tests, ruff and mypy clean, verified end to end in a browser on a seeded synthetic calibration. **The live half is outstanding and needs a human at a microphone**: no calibration read exists, so the baseline and the noise floor are proven on synthetic audio and not on a voice. | implemented |
| 2026-08-20 | `plans/2026-08-20_accent-measurement-engine.md` | **Manual test session, not a plan** — closes the live half the row above left outstanding. Room check, two real benchmark-passage reads 13 minutes apart (attempts 8, 9), and "Set the baseline" clicked in the real app: calibration computed from 276 usable tokens, noise floor **0.20 z** median across 18 vowels (0.05 z /ə/ to 0.56 z /ɚ/), vowel chart renders correctly against Hillenbrand 1995. Read 9 also produced the first live (non-fixture) capture of the `Monotone` delivery fault — 93 words across 2 stretches, coherent fallback-coach drill generated — while segmental scores held nearly flat between reads (91/98/91 both times), consistent with second-read vocal flattening rather than noise. Also ran: a shadowed benchmark read paired against the read-8 cold read, the first real shadow/cold delta the app has ever computed (Fluency -0.7, Prosody -5.4 — opposite the pre-registered direction, correctly reported as "an observation, not a result" at n=1); two real perception-trainer blocks on `/w/ → /v/` (75%, then 80%, still short of the 90%-twice graduation bar). User pushed back on the `UnexpectedBreak`/`MissingBreak` counts always reading 0 — verified directly against the raw stored JSON for all three real attempts (207 words) that Azure itself never sets `Break.ErrorTypes` to anything but `"None"`, not a parser bug; found in the process that Azure sends an unused per-word continuous confidence for both break types (`Break.UnexpectedBreak.Confidence`, `Break.MissingBreak.Confidence`) that `_prosody_detail` never reads, parallel to how `Monotone`'s confidence is already used — recorded as an opportunity, not built. | implemented |
| 2026-08-20 | `plans/2026-08-20_accent-visualisation-and-resynthesis.md` | v0.11.0, issue #14: the visible and audible half of the accent work. Six chart+table pairs on the Accent tab (rhoticity first, then the vowel quadrant with a produced→target arrow per vowel, diphthong trajectories, a semitone pitch overlay, duration, rhythm); three PSOLA resynthesis surfaces built on `parselmouth.praat.call` (corrected pitch, timing, one vowel) that change ONE dimension of the user's own recording so the variable is isolated; the arrow-to-instruction mapping moved into `vowel_reference.py` as data, because F2 responds to lip posture as strongly as to tongue advancement and the same delta needs opposite instructions for a front-unrounded and a back-rounded vowel. **The plan's own addition, beyond the issue: a General American reference this project measures itself.** Hillenbrand 1995 covers 12 of the 22 vowel categories the benchmark passage carries (rhoticity has a published mean for /ɝ/ alone), its durations are citation-form /hVd/ words that cannot be compared in ms, and it is early-1990s speech. Synthesising the benchmark passage in ~16 sex-stratified en-US neural voices and measuring each through this same pipeline costs ~15,600 TTS characters (3.1% of the monthly free tier) and ~992 STT seconds (5.5%), once, and produces a connected-speech reference complete across the inventory — populating `vowel_measure.REFERENCE_VOICE`, which has been declared since v0.10.0 with nothing behind it. Both references are kept and never averaged. Also owed and included: `scripts/rederive.py`, the re-derivation over stored audio that v0.10.0 started keeping recordings for. | implemented |
| 2026-08-20 | `plans/2026-08-20_accent-visualisation-and-resynthesis.md` | **What the capture run found, which reasoning would not have.** (1) The Speech SDK reports **no age** for a voice: `en-US-AnaNeural` is Microsoft's en-US child voice and comes back as `gender=Female, voice_type=OnlineNeural, style_list=['']`, indistinguishable from an adult woman. A child's vocal tract is far shorter, so including her would have raised the women's mean and inflated its between-voice SD — a reference wrong in a way that still draws as a plausible vowel chart. Excluded by a hand-maintained list, because there is no API to ask. (2) **The trajectory instrument was broken on connected speech and had been since v0.10.0.** The first build had FACE gliding −225 Hz where GA glides ≈ +140. Per-token dumps: across all eight men's voices only **twelve** /eɪ/ tokens clear a 90 ms floor, from three word types — "pace" (median −136 Hz), "way" (−446), "same" (−813) — and every one has a contaminating right context. "same" ends in a nasal whose murmur the 80% window reads as F1 240 Hz / F2 1285 Hz. **Not an amplitude artefact and not gateable on one**: that window measures −17.4 dB against −16.4 dB at the midpoint, i.e. the velum is open at full voicing. Consequences shipped: `MIN_TRAJECTORY_MS = 90` (derived — a 25 ms window at the 80% point needs d ≥ 62.5 ms, plus Azure's 10 ms grid), `model_reference` publishes **no** at20/at80 at all, and a travel whose sign disagrees with the reference no longer produces an instruction. (3) /ʊɹ/ CURE was the only category that failed to publish in either set — exactly what `progress_view.BENCHMARK_COVERAGE`'s own comment predicted ("treat ʊɹ as best-effort, not as covered"). Spend: **15,600 TTS characters (3.1%) and 1,023 STT seconds (5.7%)** of the monthly free tiers, once, matching the pre-flight estimate. 953 tests, ruff and mypy clean. **The live half is outstanding**: no human has read into these charts, played a corrected-pitch clip, or clicked a bridging phrase through to a drill. | implemented |
| 2026-08-20 | `plans/2026-08-20_accent-visualisation-and-resynthesis.md` | **Review pass on v0.11.0 before tagging, and what it caught.** Eight findings, five of them wrong advice or dead wiring rather than style; all fixed, each with a test that fails against the bug it covers. (1) **The rhoticity instruction was inverted.** `instruction_for(vowel, "F3", -delta)` negated a delta that was already `target − produced`, so a speaker measured 500 Hz short of the r-colouring target was told to "release the tongue bunching" — the exact failure the hand-written `ARTICULATION` table was added to prevent, and the opposite of what the generated string it replaced had said. The 150 Hz threshold beside it became `RHOTICITY_TOLERANCE_HZ`, applied symmetrically so both directions of the lookup are reachable. (2) **`ranked_gaps` read a missing `f2_travel_hz` as `or 0.0`**, manufacturing a top-ranked "glides 0 Hz — flattening toward a monophthong" drill out of the very measurement `_trajectory_findings` refuses to instruct on; connected speech is full of sub-90 ms diphthongs, so this fired on ordinary readings and fed the coach and the practice queue. (3) **Praat reads an EMPTY `Extract part` range as the whole sound** — `Extract part 0.0 0.0` on a 2 s clip returns 2 s — so `corrected_vowel` on a vowel at either edge concatenated the entire recording back in and the listener heard the sentence twice, breaking the one promise the surface makes. (4) `corrected_pitch` ran a full `to_pitch` per contour point: measured at **109 s** for a 60 s paragraph inside one Streamlit callback, now **0.37 s**. (5) `pitch_frame` never quantised the warped model timestamps, so `median(semitones)` grouped by an x unique per voice per frame and the "averaged across voices" line aggregated nothing. Three findings were decisions rather than patches, all taken: **rhoticity now scores against `model_reference` and only in hertz** (`_hertz_reference` — a z-score is relative to the inventory that produced it, so position and trajectory stay published while F3−F2, which carries no normalisation, uses the table that covers all seven r-coloured categories instead of Hillenbrand's lone /ɝ/; the chart and the table under it had been disagreeing by 200 Hz), `ranked_gaps` stopped skipping every vowel with no published mean (which had confined the rhoticity ranking to NURSE), and `_correct_worst_vowel` now ranks in z and maps the target back through the new `Normaliser.hz` rather than picking whichever vowel carried the largest vocal-tract-length offset. **And the loop was not closed:** nothing in `src/` called `with_geometry` or `ranked_gaps` — both were reachable only from tests — so `compact()`'s empty `vowel_geometry` was what both coaches saw, and the bridging phrases, the one-click drill and the queue promotion never rendered on any attempt. `app.geometry_gaps` now derives the ranking once, inside the session-cache guard and before the coach branch so both coaches see the same evidence, with `rhythm_gap` joining it as a vowel-less whole-reading entry that cannot license a phrase. Verified end to end against a seeded baseline: two trajectory gaps in, two hand-written bridging phrases out. `vowel_reference` caveat 3 amended, since the duration chart now plots those milliseconds deliberately and never alone. 974 tests, ruff and mypy clean. **Still outstanding: no human has read into these charts or played a corrected-pitch clip.** | implemented |
| 2026-08-20 | _no plan file (manual test session)_ | **First human pass over the v0.11.0 surfaces, driven live in the browser.** Confirmed working against a real voice: all six chart+table pairs on a stored reading, the model-reading capture, corrected pitch (the speaker confirmed by ear that it still sounds like them, closing the one claim the formant-preservation check could not settle), and the Progress tab's separation of benchmark / free practice / shadowed series. Four findings, none of which a test suite would have produced, all recorded in `progress.md`. (1) **Simultaneous shadowing is unusable for this speaker at either speed** — they cannot listen and speak at once, and said so unprompted; echo mode works but has no recorder at all, so the mode they can sustain captures nothing while the mode that produces data is one they have now failed at twice. The −5.4 prosody on the first shadow pair is a real signal, not fatigue: the two cold reads either side of it were flat (83.4 → 83.5, then 78.1), and the read was on headphones so no synthesiser reached the mic. Accuracy also fell 2.1, which `SHADOW_METRICS` is designed not to compare. (2) **The practice queue never rotates** — `next_due` is written back only on a state change (`app.py:3458`), so a target that stays active keeps the timestamp it was added with and a stable sort pins it at index 0 forever; `/w/ → /v/` had monopolised the block slot across two completed blocks despite carrying the least evidence of the three targets, and `last_seen` is written in two places and read in none. (3) **A repeated word is reported as a phoneme substitution** — a stumbled "Wednesday" rendered as `/eɪ/ → sounded like /w/`, advice to drill a substitution that never happened, while the diff beside it correctly showed the insertion. (4) **The error-count badges mix units**, counting one 28-word monotone stretch the same way they count two independently mispronounced words. Also: `UnexpectedBreak`/`MissingBreak` failed to appear for a third deliberate time and should now be treated as a property of Azure rather than bad luck, and `Monotone` got its second live confirmation. Nothing was fixed; the direction on shadowing was deliberately left open. Suite green at 974 passed. **Then a fifth, found after the row above was written and the worst of the set:** the Accent tab rendered attempt 12's rhoticity figures (805 Hz n=2 / 866 Hz n=1) under attempt 10's label (`#10 · 138 tokens`), and the mismatch survived a tab switch, correcting only when the selector was operated. `measurement_for` is not at fault; the selectbox at `app.py:2043` has no `index=` and a new attempt is prepended, so display and return value disagree on the render where `options` grows. High severity and silent — it misattributes acoustics on the one surface built to compare readings over time, and it fires exactly when a new attempt lands. | implemented |
| 2026-08-20 | `plans/2026-08-20_manual-session-defects.md` | Fix issues 1-4 from the manual session: the Accent tab's reading/label mismatch, the practice queue that never rotates, a repeated word reported as a phoneme substitution, and the error-count badges mixing words with spans. Issue 5 (shadowing) is deliberately left open and untouched. Each fix carries a test that fails against the bug it covers. **Issue 1 was diagnosed wrong when it was filed and the reproduction is what showed it.** Not "the render where `options` grows" and not dependent on a new attempt at all: `st.rerun()` builds `RerunData` with no `widget_states`, a `RerunException` is not a premature stop, and any rerun raised in Today or Practice ends the script before the Accent tab renders — so the selector is stale on that pass, its stored value is deleted, and the next full run re-registers it on the positional default (the newest reading) without telling the browser, which goes on painting the old label. It had been firing on every early-terminated rerun since the picker existed; it only became visible once the newest attempt stopped being the one already selected. Fixed with an identity-resolved `index=` off a plain session key, plus `label_matches_measurement` as a refusal and a caption sourced from the loaded measurement. Issue 2 took the preferred fix (`due()` orders on `last_seen`, a field written in two places and read in none). Issue 3 turned out to be wiring rather than detection — the diff already produces the Insertion — and the suppression reaches `fallback_coach.compact` as well as the card, so the two surfaces cannot disagree. Issue 4 counts `delivery_faults`' own `runs`. Suite 974 → 998; verified live in a browser for issue 1. Left alone: shadowing, and the fact that a stress drill has no completion event so `drills[0]` still cannot rotate. | implemented |
| 2026-08-21 | `plans/2026-08-21_unscripted-and-content-scores.md` | **Mode C — unscripted assessment, two-pass diagnosis, and content scores.** Milestone v0.12.0, closing #12 in full. Verified by introspection BEFORE planning: `enable_content_assessment_with_topic` is absent from azure-cognitiveservices-speech 1.51.1 because Microsoft **retired** content assessment at SDK 1.46.0, not because it was renamed — `dir(PronunciationAssessmentConfig)` has 7 members and no content method, `PronunciationAssessmentResult` no content scores, `PropertyId` no content entry, and a string scan of the native `.so` finds only referenceText/gradingSystem/granularity/enableMiscue/enableProsodyAssessment/phonemeAlphabet/nbestPhonemeCount. The JSON route DOES still carry unknown keys (`PronunciationAssessmentConfig(json_string=…)` round-trips `enableContentAssessment`/`contentTopic` back out of `to_json()`), so one deliberate probe answers whether the service still responds; the standing route is Gemini on Microsoft's own published rubric, labelled as such. Two-pass (standard STT for an accurate transcript, then scripted assessment against it) because a phoneme diagnosis against a wrong transcript blames the wrong sounds. The budget guard was verified rather than rebuilt — `passes_for`/`preflight_stt` already cover both passes before the first is sent; the one real gap is post-hoc, fixed by summing both passes into `Assessment.attempts`. Register separation is the scientific half: per-style baselines, `plot_gate` refusing a style mismatch instead of warning, a mode-aware per-vowel token floor, and the style tag beside the token count in the four-column table. | planned |
